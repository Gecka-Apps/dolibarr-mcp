# Core MCP dependencies
# Capped below 2.x: that release replaces the low-level decorator API this
# server is built on.
mcp>=1.27,<2

# HTTP and async support
aiohttp>=3.9.0

# Data validation and settings
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# CLI support
click>=8.1.0

# HTTP server runtime for Streamable HTTP transport
uvicorn>=0.25.0
starlette>=0.37,<2

# Analytics (database access for aggregation queries)
aiomysql>=0.2.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
